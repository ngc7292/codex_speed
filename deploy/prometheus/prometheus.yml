global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: codex-speed
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - codex-speed:9467
